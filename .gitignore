__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# generated build output (regenerable via render.render_html / sync.py)
*.html
# exception: outline-wiki's sample fixture keeps its rendered HTML checked
# in as a working example of the standalone-HTML export
!outline-wiki/example/sample.html

# per-sync state: a point-in-time snapshot, not meant to be durable
# history (the attachment manifest itself *is* tracked; see design.md)
*.outline-wiki-metadata.json
