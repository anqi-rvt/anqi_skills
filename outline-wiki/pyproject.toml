[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.ruff.lint.isort]
known-first-party = ["outline_client", "manifest", "anchors", "render", "sync", "validate"]

[tool.pytest.ini_options]
pythonpath = [".", "src"]
